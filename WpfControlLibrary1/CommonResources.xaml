<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- Border hover color -->
    <Style x:Key="engineBorderHoverColor" TargetType="Border">
        <Setter Property="BorderBrush" Value="Black"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#FF3E65A8"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <!-- Engine header background style -->
    <Style x:Key="EngineStackPanelHeaderStyle" TargetType="StackPanel">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#FF0E0E0E"/>
                    <GradientStop Offset="0.5" Color="#FF3A3939"/>
                    <GradientStop Offset="1" Color="#FF535252"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EngineDockPanelHeaderStyle" TargetType="DockPanel">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#FF0E0E0E"/>
                    <GradientStop Offset="0.5" Color="#FF3A3939"/>
                    <GradientStop Offset="1" Color="#FF535252"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!-- Engine label size 1 -->
    <Style x:Key="engineLabelStyle_Size1" TargetType="Label">
        <Setter Property="Foreground" Value="#FFC1C1C1"/>
        <Setter Property="FontSize" Value="19" />
        <Setter Property="FontFamily" Value="Comic Sans MS, Verdana"/>
    </Style>

    <!-- Engine label header -->
    <Style x:Key="engineLabelStyle_header" TargetType="Label">
        <Setter Property="Foreground" Value="#FFC1C1C1"/>
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="Comic Sans MS, Verdana"/>
    </Style>

    <!-- Engine label size 2 -->
    <Style x:Key="engineLabelStyle_Size2" TargetType="Label">
        <Setter Property="Foreground" Value="#FFC1C1C1"/>
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontFamily" Value="Comic Sans MS, Verdana"/>
    </Style>
    
    <!-- Engine textblock size 2 -->
    <Style x:Key="engineTextBoxStyle_Size2" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#FFC1C1C1"/>
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontFamily" Value="Comic Sans MS, Verdana"/>
    </Style>

    <!-- Engine listbox background style -->
    <Style x:Key="EngineListBoxStackPanelBackground" TargetType="StackPanel">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#FF0E0E0E"/>
                    <GradientStop Offset="0.5" Color="#FF252525"/>
                    <GradientStop Offset="1" Color="#FF424242"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EngineListBoxDockPanelBackground" TargetType="DockPanel">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Offset="0" Color="#FF0E0E0E"/>
                    <GradientStop Offset="0.5" Color="#FF252525"/>
                    <GradientStop Offset="1" Color="#FF424242"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    
    
    
    
    
    
    
    
    
    
    
    
    
    <!-- Engine button -->

    <Style x:Key="ShadowStyle">
        <Setter Property="Control.Foreground" Value="Black" />
    </Style>

    <!-- Button focus style -->
    <Style x:Key="MyFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Control}">
                    <Grid Margin="3 2">
                        <Rectangle Name="r1" StrokeThickness="1" Stroke="Black" StrokeDashArray="2 2"/>
                        <Border Name="border" Width="{TemplateBinding ActualWidth}" Height="{TemplateBinding ActualHeight}"  CornerRadius="2" BorderThickness="1" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Common button style -->
    <Style x:Key="listBoxEntryButton" TargetType="Button">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="FontFamily" Value="Verdana"/>
        <Setter Property="FontSize" Value="11px"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FocusVisualStyle" Value="{StaticResource MyFocusVisual}" />
        <Setter Property="Foreground" Value="#FFC5BDBD" />
        <Setter Property="Background" >
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1" >
                    <GradientStop Color="#FF0E0E0E" Offset="0.2"/>
                    <GradientStop Color="#FF252525" Offset="0.85"/>
                    <GradientStop Color="#FF424242" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Name="border"
               BorderThickness="1"
               Padding="4,2"
               BorderBrush="DarkGray"
               CornerRadius="3"
               Background="{TemplateBinding Background}">
                        <Grid >
                            <ContentPresenter HorizontalAlignment="Center"
                              VerticalAlignment="Center" Name="contentShadow"
                   Style="{StaticResource ShadowStyle}">
                                <ContentPresenter.RenderTransform>
                                    <TranslateTransform X="1.0" Y="1.0" />
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                            <ContentPresenter HorizontalAlignment="Center"
                           VerticalAlignment="Center" Name="content"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#FF4788c8" />
                            <Setter Property="Foreground" Value="#FF4788c8" />
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" >
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1" >
                                        <GradientStop Color="#FF0E0E0E" Offset="0.35"/>
                                        <GradientStop Color="#FF252525" Offset="0.95"/>
                                        <GradientStop Color="#FF424242" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter TargetName="content" Property="RenderTransform" >
                                <Setter.Value>
                                    <TranslateTransform Y="1.0" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsDefaulted" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#FF282828" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="#FF282828" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.7" />
                            <Setter Property="Foreground" Value="Gray" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>